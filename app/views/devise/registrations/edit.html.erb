<h2>アカウント編集</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "mb-3" }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.label :name, "名前", class: 'form-label' %>
    <%= f.text_field :name, autofocus: true, autocomplete: "name", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :email, "メールアドレス", class: 'form-label' %>
    <%= f.email_field :email, autocomplete: "email", class: 'form-control' %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>現在、次のメールアドレスの確認を待っています： <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group">
    <%= f.label :password, "パスワード", class: 'form-label' %> <i>(変更しない場合は空白のままにしてください)</i>
    <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
    <% if @minimum_password_length %>
      <small class="form-text text-muted"><%= @minimum_password_length %> 文字以上必要です。</small>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation, "パスワード確認", class: 'form-label' %>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :current_password, "現在のパスワード", class: 'form-label' %> <i>(変更を確認するため、現在のパスワードが必要です)</i>
    <%= f.password_field :current_password, autocomplete: "current-password", class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "更新", class: 'btn btn-primary' %>
  </div>
<% end %>

<h3>アカウントのキャンセル</h3>

<p>もしアカウントをキャンセルしたい場合は、以下のボタンをクリックしてください。</p>
<div>
  <%= button_to "アカウントのキャンセル", registration_path(resource_name), data: { confirm: "本当によろしいですか？" }, method: :delete, class: 'btn btn-danger' %>
</div>

<%= link_to "戻る", :back, class: 'btn btn-link' %>
